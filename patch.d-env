#!/bin/sh

tmp=/tmp/system-supersu

console=$(cat /tmp/console)
[ "$console" ] || console=/proc/$$/fd/1

cd "$tmp"
. config.sh

print() {
	echo "ui_print -- $1" > $console
	echo
}

abort() {
	[ "$1" ] && print "Error: $1"
	exit 1
}

cd "$ramdisk"
